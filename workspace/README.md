# Workspace

This directory is mounted at `/workspace` by the example configuration.

For a real deployment, set `WORKSPACE_PATH` in `.env` to the host directory
containing the repositories that Codex may access.
